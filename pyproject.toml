[project]
name = "bajki-generator"
version = "0.1.0"
description = "Prosty generator bajek tekstowych po polsku"
readme = "README.md"
authors = [{ name = "Fluxify", email = "contact@example.com" }]
requires-python = ">=3.11"

[project.optional-dependencies]
cli = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
