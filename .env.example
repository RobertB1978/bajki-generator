# Backend (apps/) configuration - copy to .env in project root
BAJKI_API_PREFIX=/api
BAJKI_FRONTEND_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
BAJKI_DEFAULT_LANGUAGE=pl

# Frontend (web/) configuration - copy to web/.env.local or similar
VITE_API_BASE_URL=/api
